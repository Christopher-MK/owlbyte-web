---
import BaseLayout from '@/layouts/BaseLayout.astro';
import ProcessTimeline from '@/components/ProcessTimeline.astro';
import CTA from '@/components/CTA.astro';

const title = 'Proceso | OwlByte';
const description = 'Metodología clara y colaborativa para diseñar, construir y optimizar proyectos digitales.';
---

<BaseLayout title={title} description={description}>
  <section class="bg-slate-50">
    <div class="mx-auto max-w-6xl px-6">
      <div class="container-grid">
        <div class="lg:col-span-5">
          <h1 class="text-4xl font-semibold text-navy-900">Un proceso claro, flexible y medible</h1>
          <p class="mt-4 text-base text-slate-600">
            Trabajamos con equipos internos de forma transparente, priorizando entregables rápidos y validables.
          </p>
        </div>
        <div class="lg:col-span-7">
          <ProcessTimeline />
        </div>
      </div>
    </div>
  </section>

  <section>
    <div class="mx-auto max-w-6xl px-6">
      <div class="grid gap-6 md:grid-cols-3">
        <div class="card p-6">
          <h3 class="text-lg font-semibold text-navy-900">Alineación semanal</h3>
          <p class="mt-3 text-sm text-slate-600">
            Reuniones breves con avances visibles para mantener el ritmo y la claridad.
          </p>
        </div>
        <div class="card p-6">
          <h3 class="text-lg font-semibold text-navy-900">Documentación viva</h3>
          <p class="mt-3 text-sm text-slate-600">
            Entregamos guías y handoffs para que tu equipo continúe creciendo.
          </p>
        </div>
        <div class="card p-6">
          <h3 class="text-lg font-semibold text-navy-900">QA multi-dispositivo</h3>
          <p class="mt-3 text-sm text-slate-600">
            Validaciones en móvil, tablet y desktop antes del lanzamiento.
          </p>
        </div>
      </div>
    </div>
  </section>

  <CTA
    title="Construyamos juntos tu próximo proyecto"
    description="Te acompañamos desde la idea hasta el lanzamiento y la optimización continua."
    primaryLabel="Ver disponibilidad"
  />
</BaseLayout>
