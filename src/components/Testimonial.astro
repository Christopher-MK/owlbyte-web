---
const { name, role, quote } = Astro.props;
---

<div class="card p-6">
  <p class="text-sm text-slate-600">“{quote}”</p>
  <div class="mt-4">
    <p class="text-sm font-semibold text-navy-900">{name}</p>
    <p class="text-xs text-slate-500">{role}</p>
  </div>
</div>
